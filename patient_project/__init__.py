from flask_sqlalchemy import SQLAlchemyimport flaskimport osfrom flask_migrate import Migratefrom flask_login import LoginManager# TODO / in this file the order is very important be care full !!! /.# TODO / in this file the order is very important be care full !!! /.app = flask.Flask(__name__)app.config['SQLALCHEMY_DATABASE_URI'] = "sqlite:///"+ os.path.join(os.path.dirname(__file__), 'patients.db')app.config['SECRET_KEY']  = "sla;fdasdgfteabsbdalfsdjk"db = SQLAlchemy(app)migrate = Migrate(app, db)login_manager = LoginManager(app)  # initiate the classfrom patient_project import routes, models